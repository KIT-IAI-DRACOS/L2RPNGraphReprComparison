defaults:
  - experiment: default
  - training: ppo
  - model: gnn
  - obs_space: graph
  - reward: constant
  - env: case14
  - opponent: disabled
  - evaluation: default
  - rollouts: default
  - callbacks: default
  - optimization: disabled
  - _self_

hydra:
  job:
    chdir: false
  run:
    dir: ${experiment.storage_path}/${experiment.name}
